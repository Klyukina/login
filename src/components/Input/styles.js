import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
        borderWidth: 1,
        width: 250,
        height: 50,
        padding: 10,
        marginBottom: 25
    }
});